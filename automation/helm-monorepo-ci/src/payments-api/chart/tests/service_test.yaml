suite: Service
templates:
  - templates/service.yaml

tests:
  - it: should create a Service
    asserts:
      - isKind:
          of: Service

  - it: should use configured service port
    set:
      service.port: 8080
    asserts:
      - equal:
          path: spec.ports[0].port
          value: 8080
